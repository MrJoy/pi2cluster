# hostname for your HypriotOS device
hostname: pi2-n<%= "%02d" % node.to_i %>
docker:
  images:
    - "/var/local/rpi-consul_v0.6.4.tar.gz"
    - "/var/local/rpi-swarm_v1.2.2.tar.gz"
clusterlab:
  service:
    run_on_boot: "true"

# optional wireless network settings
wifi:
  interfaces:
#     wlan0:
#       ssid: "MyNetwork"
#       password: "secret_password"
